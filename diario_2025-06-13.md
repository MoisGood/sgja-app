# Diario — 13 Junio 2025

## Sesión interrumpida por cambio de modelo (Big Pickle → DeepSeek V4 Flash)

---

## Objetivo principal de la sesión

Unificar el header mobile en todas las páginas TÉCNICO y rediseñar la página de escaneo QR siguiendo la referencia `stitch_edutech_ops_pro/escaneo_qr_modo_*`.

---

## Avances concretos

### 1. Header unificados (páginas secundarias)

Se agregó header inline (stitch-style) a:

- **`src/pages/MobileInventario.tsx`** — back arrow + "Soporte TI" (sin lupa)
- **`src/pages/MobileConfigTecnico.tsx`** — back arrow + "Soporte TI" (sin lupa)
- **`src/pages/MobileQrScanner.tsx`** — header comentado, pendiente (ver sección QR)

### 2. UsuariosOnlineIndicador activo en MobileDashboard

Se movió el indicador de usuarios conectados/desconectados al header de `MobileDashboard.tsx` junto con toggle de tema oscuro y menú hamburguesa.

### 3. Limpieza de MobileLayout

Se eliminaron imports y variables no usados (supabase, registrarCierre, UsuariosOnlineIndicador, useTheme, handleLogout) del archivo `MobileLayout.tsx` para que el build pase limpio.

### 4. Análisis de la página QR

Archivo: `src/pages/MobileQrScanner.tsx`

**Estado actual (sin implementar, código original):**
- Abre cámara con `html5-qrcode`
- Al escanear: navega a `/tecnico/qr?c=CODIGO`
- Info card hardcodeada ("Sala de Computación 1", 15 PCs, 2 tickets) siempre visible
- Linterna solo icono en header comentado, sin funcionalidad
- "Técnico de Soporte" texto fijo

**Requerimientos capturados del usuario:**
1. Cámara se abre para escanear QR (equipo o lugar)
2. No debe aparecer nada (ni info card) hasta capturar QR
3. Al capturar: buscar código en tabla `qr_codes` → resolver `tipo` ('lugar'/'equipo') → obtener `id_referencia`
4. Mostrar datos reales: nombre del lugar, cantidad de equipos, cantidad de tickets activos
5. Tickets activos clickeables → navegar a `/ticket?ticket=ID` para cerrar/completar
6. Linterna toggle vía `torch` del `MediaStreamTrack`
7. "Ver Equipos" → `/tecnico/m/equipos?id_lugar=ID`. "Crear Ticket" → `/ticket?lugar=ID`
8. "Técnico de Soporte" = nombre del usuario logueado

**Diseño referencia:** `stitch_edutech_ops_pro/escaneo_qr_modo_claro/code.html`

---

## Archivos modificados

| Archivo | Cambio |
|---|---|
| `src/pages/MobileDashboard.tsx` | Header: build + Soporte TI + punto verde + luna + menú |
| `src/components/MobileLayout.tsx` | Limpieza de imports/variables no usados |
| `src/pages/MobileInventario.tsx` | Header inline stitch-style (back + Soporte TI, sin lupa) |
| `src/pages/MobileConfigTecnico.tsx` | Header inline stitch-style (back + Soporte TI, sin lupa) |
| `src/pages/MobileQrScanner.tsx` | **Revertido al original** (solo analizado, pendiente implementación) |

---

## Pendiente para próxima sesión

- [ ] Implementar QR page según diseño `escaneo_qr_modo_claro/code.html`:
  - [ ] Cámara escanea → lookup en `qr_codes` → resuelve lugar
  - [ ] Info card con datos reales: desktop_windows + equipos count, confirmation_number + tickets count
  - [ ] Lista de tickets clickeables (si hay) entre stats y acciones
  - [ ] Botones apilados verticalmente: "Ver Equipos" (inventory icon) + "Crear Ticket en esta Sala" (add_circle icon)
  - [ ] Linterna (torch real) en esquina inferior derecha del visor
  - [ ] Técnico logueado abajo del info card
  - [ ] Header con QR icon en derecha para escanear otro
- [ ] Revisar si header de `MobileTickets.tsx` debe unificarse también
- [ ] El header ficito ("EduTech Support") de MobileQrScanner quedó comentado — decidir si se elimina

---

## Notas técnicas

- Tabla `qr_codes`: columnas `codigo`, `tipo` ('lugar'|'equipo'), `id_referencia`, `activo`
- Si `tipo = 'equipo'`: hacer lookup secundario en `equipos` para obtener `id_lugar`
- Fallback: buscar por nombre en `lugares` con `ilike`
- Linterna: `track.applyConstraints({ advanced: [{ torch: true }] })` sobre video element de html5-qrcode
- Tickets activos: `requerimientos WHERE id_lugar = ? AND activo = true AND estado NOT IN ('Completada','Cancelada')`
